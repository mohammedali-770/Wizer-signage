# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
.next/
out/
build/
coverage/
.turbo/

# Lockfiles
pnpm-lock.yaml

# Android (Kotlin/Gradle formatted by their own tooling)
apps/android-tv-player/

# Generated / vendor
*.min.js
*.min.css

# Generated contracts. These are byte-compared in CI against a fresh emit
# (scripts/emit-openapi.ts, and the manifest goldens read by both the API and
# the Android player), so reformatting them makes the drift check fail on a
# difference that means nothing. Prettier collapsing short arrays here already
# broke it once.
contracts/
