<?xml version="1.0" encoding="utf-8"?>
<!--
  Dedicated signage devices are paired identities, not user-content clients.
  Nothing in app-private storage is portable to a second TV: it includes device
  credentials, pairing state, local cache indexes and operational state.

  Android 11 and lower use this legacy full-backup format. Android 12+ uses
  data_extraction_rules.xml instead.
-->
<full-backup-content>
    <exclude domain="root" path="." />
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="external" path="." />
    <exclude domain="device_root" path="." />
    <exclude domain="device_file" path="." />
    <exclude domain="device_database" path="." />
    <exclude domain="device_sharedpref" path="." />
</full-backup-content>
